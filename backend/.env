# Server Port
PORT=5000

# MongoDB Connection URI (Replace with actual database URL)
MONGO_URI=mongodb://localhost:27017/vibie

# JWT Secret Key (Must be a strong, unique key)
JWT_SECRET=your-very-secret-key

# Allowed Frontend URL (Update this for production)
CLIENT_URL=http://localhost:3000